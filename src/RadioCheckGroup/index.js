import styled from "@emotion/styled";

import RadioCheckGroup from "./RadioCheckGroup";

export default styled(RadioCheckGroup)`
  display: inline-block;
  vertical-align: middle;
  & > label {
    margin-bottom: 5px;
  }
`;
